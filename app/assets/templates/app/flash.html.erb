<div ui-if='!_.isEmpty(app.alerts.length)' class="container">
	<div class="row">
		<div class="offset6 span6">
			<alert ng-repeat="(id, alert) in app.alerts" type="alert.type" close="app.closeAlert(id)">{{alert.msg}}</alert>
		</div>
	</div>
</div>