%header#header.navbar
	.navbar-inner
		.container
			%ul.nav.user-nav.pull-right
				-if current_user
					%li.dropdown
						%a.dropdown-toggle{:href => "#", :role => "button", "data-toggle" => "dropdown"}
							-if current_user.avatar
								=image_tag current_user.avatar.image.thumb.url, class: 'img-circle', width: 20, height: 20
							= current_user.username
							%i.icon-caret-down
							.dropdown-menu
								-# %li= link_to "Settings", settings_path
								%li.divider
								-# %li= link_to "Logout", logout_path
				-else
					%li
						=link_to "Login", new_user_session_path


			=link_to home_pages_path, class: "brand" do
				+ _____
